<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Image en fond -->
    <ImageView
        android:id="@+id/imageMamouthChoose"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/backgroundmammouth2" />

    <!-- Colonne de boutons par-dessus -->
    <LinearLayout
        android:id="@+id/buttonContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|center_vertical"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_marginEnd="32dp">

        <Button
            android:id="@+id/buttonListenStory"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Ecouter l’histoire"
            android:background="@drawable/button_background"
            android:textColor="#000000"
            android:textStyle="bold"
            android:layout_marginBottom="16dp"
            android:minWidth="200dp"
            android:minHeight="64dp" />

        <Button
            android:id="@+id/buttonInteractions"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Interactions"
            android:background="@drawable/button_background"
            android:textColor="#000000"
            android:textStyle="bold"
            android:layout_marginBottom="16dp"
            android:minWidth="200dp"
            android:minHeight="64dp" />

        <Button
            android:id="@+id/buttonChatGptFeedback"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Chat-GPT feedback"
            android:background="@drawable/button_background"
            android:textColor="#000000"
            android:textStyle="bold"
            android:minWidth="200dp"
            android:minHeight="64dp" />

    </LinearLayout>

</FrameLayout>
